{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agenttrust.example/platform-sre/sre-readiness.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "ready", "database_ready", "orchestrator_ready", "effect_adapters_ready", "production_certification"],
  "properties": {
    "schema_version": {"const": "agenttrust.sre-readiness.v1"},
    "ready": {"const": true},
    "database_ready": {"const": true},
    "orchestrator_ready": {"const": true},
    "effect_adapters_ready": {"const": true},
    "production_certification": {"const": false}
  }
}
