-r python/durable_worker/requirements.production.txt

# Validate every public OpenAPI document as a complete OpenAPI 3.x contract.
openapi-spec-validator==0.9.0
# Registry wire-contract compatibility tests validate the Draft 2020-12 source schema directly.
# Keep this at the validator's minimum compatible release to limit CI dependency drift.
jsonschema==4.26.0
